
package contadorhorastarefa.DAO;

/**
 *
 * @author Fabinhu
 */
public class AtualizarTabelas extends BancoDeDados{
    
    /*
    1 - Comando para Criar banco de dados
    CREATE DATABASE BancoContadorHoras;
    
    2 - Comando para Criar Tabelas no Banco
CREATE TABLE Usuario (
         codigo integer CONSTRAINT pk_codigo PRIMARY KEY,
         nome varchar(15) NOT NULL,
         data_adm date
    );
    
    CREATE TABLE Setor (
    codigo integer CONSTRAINT pk_codigo_setor PRIMARY KEY,
    descricao varchar(10)
    );
    
    CREATE TABLE Cargo (
    codigo integer CONSTRAINT pk_codigo_cargo PRIMARY KEY,
    descricao varchar(10)
    );
    
     CREATE TABLE Versao (
    codigo integer CONSTRAINT pk_codigo_versao PRIMARY KEY,
    descricao varchar(10),
    data_atualizacao date
    );
    */
}
